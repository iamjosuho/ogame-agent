schema_version = 1

# Default base values. Replace with your account's actual cargo capacities
# (after Hyperspace Technology + Collector class bonus + Lifeform tech bonuses).
# Account facts, not game defaults. Update these values whenever cargo-affecting
# technology, class, or Lifeform bonuses change. No code may infer a fallback.
[cargo_capacities]
"202" = 5000   # Small Cargo (base capacity: 5,000)
"203" = 25000  # Large Cargo (base capacity: 25,000)
